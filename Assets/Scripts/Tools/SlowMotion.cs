using UnityEngine;

public class SlowMotion : MonoBehaviour
{
    [SerializeField] int[] multiplierArray = new int[] {1,2,4,8};

    // Update is called once per frame
    void Update()
    {
        
    }
}
