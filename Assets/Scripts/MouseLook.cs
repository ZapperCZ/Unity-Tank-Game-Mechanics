using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MouseLook : MonoBehaviour
{
    float mouseX;

    void Start()
    {
        mouseX=0;
    }

    // Update is called once per frame
    void Update()
    {
        mouseX = Input.GetAxis
    }
}
